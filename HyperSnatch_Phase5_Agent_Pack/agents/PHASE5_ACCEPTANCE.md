Phase 5 complete only if:

Timeline view renders from HAR

Stream ladder reconstructs bitrate levels

Waterfall graph displays segment requests

CDN detection identifies known providers

All UI buttons trigger real logic
